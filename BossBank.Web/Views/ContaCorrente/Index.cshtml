@model IEnumerable<BossBank.Data.Entities.ContaCorrente>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AgenciaCc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SaldoCc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreditoCc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AgenciaCc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SaldoCc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreditoCc)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ContaCc">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ContaCc">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ContaCc">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
